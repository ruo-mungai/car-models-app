import React from 'react'
import Filter from './Filter'


function NavBar({styles}) {
  return (
    <div>
         <h1 className={styles}>React Car App</h1>
         <Filter /></div>
  )
}

export default NavBar